before_script:
  - node -v
  - npm -v

build:
    stage: build
    script:
        - npm install
        - npm run build
  
test:
    stage: test
    script:
        - npm run test

deploy:
    state: deploy
    script:
        - return 0